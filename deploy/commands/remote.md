---
description: "Bật/tắt terminal từ xa, và ghép cặp điện thoại. `/remote on <tên>` đặt tên phiên; `/remote pair` chờ điện thoại xin ghép rồi tính số so sánh (không in ra); `/remote pair xac-nhan <số>` gõ đúng số đọc được TRÊN ĐIỆN THOẠI để hoàn tất ghép — máy này so và quyết định, không phải điện thoại; `/remote devices` và `/remote unpair <số>` quản lý thiết bị đã ghép"
allowed-tools: Bash(node:*)
---

## Kết quả

!`node "{{CCRC_REPO}}/term/bin/ccrc-term-cli.js" $ARGUMENTS`

## Nhiệm vụ của bạn (Claude)

Thuật lại ngắn gọn kết quả ở trên cho người dùng. Nếu có dòng cảnh báo (⚠) hoặc
báo lỗi (✗), nói rõ người dùng cần làm gì để sửa. Không làm gì khác.
